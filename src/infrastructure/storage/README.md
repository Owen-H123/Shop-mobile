# storage

Local persistence adapters (AsyncStorage, SecureStore, MMKV, etc.) implementing repository interfaces defined in `domain/repositories`, the same way `infrastructure/repositories` does for the remote API.
