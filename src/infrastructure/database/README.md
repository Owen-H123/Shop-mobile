# database

Conexión y setup de SQLite (`expo-sqlite`), migraciones y creación de tablas. Los repositorios en `infrastructure/repositories` usan lo que se exponga aquí para implementar los contratos definidos en `domain/repositories`.

TODO: agregar dependencia `expo-sqlite` (`pnpm add expo-sqlite`) y el cliente de base de datos cuando se implemente el CRUD real.
